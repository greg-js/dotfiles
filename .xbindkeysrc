# Increase volume
"pactl set-sink-volume 0 +5%"
  XF86AudioRaiseVolume

# Decrease volume
"pactl set-sink-volume 0 -5%"
  XF86AudioLowerVolume

# Mute volume
"pactl set-sink-mute 0 toggle"
  XF86AudioMute
